---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: demo-rccl-hvac
  namespace: demo-rccl-hvac
spec:
  replicas: 1
  selector:
    matchLabels:
      demo: rccl-hvac
  serviceName: demo
  template:
    metadata:
      labels:
        demo: rccl-hvac
    spec:
      containers:
        - image: DOCKER_IMAGE
          imagePullPolicy: Always
          name: demo
          ports:
            - containerPort: 9001
              protocol: TCP
          resources:
            limits:
              cpu: "2"
              memory: 4Gi
            requests:
              cpu: "2"
              memory: 2Gi
